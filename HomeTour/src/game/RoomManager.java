package game;

public class RoomManager {
	/*
	 * contains the init() method that will be called in Main
	 * this will instantiate all Room objs, link them as exits and 
	 * designate a starting room
	 * 
	 * Room startingRoom; self explanatory
	 * Room[] rooms; an index of all rooms in the house
 	 */
}
