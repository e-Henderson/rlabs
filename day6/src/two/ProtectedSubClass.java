package two;

import one.ProtectedClass;

public class ProtectedSubClass extends ProtectedClass {
	/*
	 * the extends keyword allows this new class to use the ProtectedClass
	 * The ProtectedClass also needed to have it's package to be imported
	 * to be able to use the protected portions. ProtectedSubClass is a child
	 * class or subclass of ProtectedClass which is the parent class.
	 */
	
}
